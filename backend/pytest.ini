[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: calls real external APIs (Gemini, Atlas) — skipped by default
    slow: takes more than 5 seconds
addopts = -m "not integration"
